import styled from 'styled-components';

const ContentTitleStyled = styled.h2`
  padding: 30px 0;
`;

export const ContentTitle = () => {
  return <ContentTitleStyled>Все пиццы</ContentTitleStyled>;
};
